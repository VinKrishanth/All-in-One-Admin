// import express from "express";
// import { uploadMiddleware, addNotice } from "../controllers/noticeController.js";

// const router = express.Router();

// // Route to add a new notice with file upload
// router.post("/add", uploadMiddleware.single("attachment"), addNotice);

// // You can uncomment and add the other routes as needed:
// // router.get("/", getNotices);
// // router.get("/:id", getNotice);
// // router.put("/update/:id", uploadMiddleware.single("attachment"), updateNotice);
// // router.delete("/delete/:id", deleteNotice);

// export default router;
